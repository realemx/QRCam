#include <opencv2/opencv.hpp>
/*
 * Draws a red box on image. Box placement is defined by
 * the vector of vertices given by cv::QRCodeDetector::detect()
 */
void drawMatch(cv::Mat image, cv::Mat points);


